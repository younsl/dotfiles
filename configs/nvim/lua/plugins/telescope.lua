return {
  'nvim-telescope/telescope.nvim',
  tag = '0.1.8',
  -- 또는 branch = '0.1.x' 사용 가능
  dependencies = {
    'nvim-lua/plenary.nvim'
  },
  config = function()
    -- Telescope 설정이 필요한 경우 여기에 추가
  end,
}