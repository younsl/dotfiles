[package]
name = "context-monitor"
version = "0.1.0"
edition = "2024"
rust-version = "1.94"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"
regex = "1"

[profile.release]
strip = true
lto = true
opt-level = "z"
